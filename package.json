{
  "name": "super-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A chat-first helper agent that mediates between users, workspaces, MCP tools, and Claude Code workers.",
  "bin": {
    "super-helper": "./dist/cli.js"
  },
  "files": [
    "dist",
    "src/agents",
    "docs",
    "AGENT.md",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "prebuild": "node scripts/check-node-version.mjs",
    "clean": "rm -rf dist",
    "build": "rm -rf dist && vite build && tsc -p tsconfig.build.json",
    "dev": "pnpm build && node dist/cli.js dev",
    "start": "pnpm build && node dist/cli.js dashboard",
    "onboard": "pnpm build && node dist/cli.js onboard",
    "onDashboard": "pnpm build && node dist/cli.js onboard",
    "dashboard": "pnpm build && node dist/cli.js dashboard",
    "status": "pnpm build && node dist/cli.js status",
    "doctor": "pnpm build && node dist/cli.js doctor",
    "predoctor": "node scripts/check-node-version.mjs",
    "knowledge:init": "pnpm build && node dist/cli.js knowledge init",
    "knowledge:update": "pnpm build && node dist/cli.js knowledge update",
    "knowledge:extract": "pnpm build && node dist/cli.js knowledge extract",
    "knowledge:normalize": "pnpm build && node dist/cli.js knowledge normalize",
    "knowledge:slice": "pnpm build && node dist/cli.js knowledge slice",
    "knowledge:audit": "pnpm build && node dist/cli.js knowledge audit",
    "knowledge:repair": "pnpm build && node dist/cli.js knowledge repair --plan",
    "knowledge:review": "pnpm build && node dist/cli.js knowledge review",
    "knowledge:publish": "pnpm build && node dist/cli.js knowledge publish",
    "accept:knowledge": "pnpm build && node dist/cli.js accept knowledge",
    "acceptance:mcp:local": "pnpm build && node --test test/mcp-runtime.test.mjs",
    "acceptance:knowledge:local": "pnpm build && node --test test/knowledge-local-acceptance.test.mjs",
    "acceptance:answer-fidelity:offline": "pnpm build && node --test test/answer-fidelity-offline-acceptance.test.mjs",
    "audit:answer-fidelity:mutations": "node scripts/verify-answer-fidelity-mutations.mjs",
    "typecheck": "tsc --noEmit && vue-tsc --noEmit -p web/tsconfig.json",
    "typecheck:web": "vue-tsc --noEmit -p web/tsconfig.json",
    "lint": "node scripts/verify-docs.mjs",
    "test": "node scripts/verify-docs.mjs && pnpm typecheck && pnpm build && node --test test/*.test.mjs",
    "test:web": "vitest run",
    "test:e2e": "pnpm build && playwright test"
  },
  "engines": {
    "node": ">=20.19.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.15.30",
    "@vitejs/plugin-vue": "^6.0.7",
    "@vue/test-utils": "^2.4.11",
    "jsdom": "^28.1.0",
    "typescript": "^5.8.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.7"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "vue": "^3.5.39",
    "vue-router": "^4.6.4",
    "zod": "^4.4.3"
  }
}
